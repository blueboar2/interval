#include <stdio.h>
#include <glib.h>

#include "simple_interval.h"

GList * simple_interval (char * neravenstvo)
    {
    GList *answer = NULL;

    printf ("%s %s", "Very simple: ", neravenstvo);
    return 0;
    }